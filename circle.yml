eneral:
  artifacts:
    -/home/ubuntu/Writter-Challenge/build/outputs/reports/androidTest/connected

machine:
  environment:
    ANDROID_HOME: /home/ubuntu/android

dependencies:
  cache_directories:
    - ~/.android
    - ~/android
  override:
    - (echo "Downloading Android SDK v19 now!")
    - (source environmentSetup.sh && getAndroidSDK)
    - (cd WritersChallenge && ./gradlew)

test:
  pre:
    - $ANDROID_HOME/tools/emulator -avd testAVD -no-skin -no-audio -no-window:
        background: true
    - (source environmentSetup.sh && waitForAVD)
  override:
    - (echo "Running JUnit tests!")
    - (./gradlew connectedAndroidTest)
